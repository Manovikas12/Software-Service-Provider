# demo/urls.py (assuming this is the urls.py for the 'demo' app)

from django.urls import path
from . import views

urlpatterns = [
    # Define the URL pattern for the chatbot processing view
    # Add other URL patterns as needed for your app
]
